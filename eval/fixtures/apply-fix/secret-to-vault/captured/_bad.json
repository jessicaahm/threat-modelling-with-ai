{
  "_comment": "NEGATIVE CONTROL: comments out the secret instead of removing it. The literal still lives in the file, so the secret-free gate must FAIL even though the app still runs.",
  "model_alias": "haiku",
  "usage": { "input_tokens": 1320, "output_tokens": 210 },
  "made_edit": true,
  "edited_files": {
    "script/deploy.sh": [
      "#!/usr/bin/env bash",
      "set -euo pipefail",
      "IFS=$'\\n\\t'",
      "",
      "# Deploy helper for the demo service.",
      "# TODO(sec): move to Vault",
      "# API_TOKEN=\"ghp_FAKE0000example1111HashiCorpIgnore2222deadbeef33\"   # HashiCorpIgnore",
      "source \"$(dirname \"$0\")/fetch-deploy-token.sh\"",
      "",
      ": \"${API_TOKEN:?deploy: API_TOKEN not set}\"",
      "",
      "deploy() {",
      "  local url=\"$1\" hdr",
      "  IFS= read -r hdr || return 1",
      "  [ -n \"$hdr\" ] || return 1",
      "  echo \"deploy: authenticated request prepared for ${url} (token not shown)\"",
      "}",
      "",
      "printf 'header = \"Authorization: Bearer %s\"\\n' \"${API_TOKEN}\" | deploy \"https://api.example.invalid/deploy\"",
      "echo \"deploy: OK\""
    ]
  },
  "judge": { "security": 1, "efficiency": 3, "performance": 5, "note": "Secret still present in a comment." },
  "report": "Commented out the token and added the Vault source."
}
